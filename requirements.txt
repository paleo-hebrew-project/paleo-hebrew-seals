# Core
PyYAML>=6.0
numpy>=1.24
Pillow>=10.0
tqdm>=4.65
pandas>=2.0
opencv-python-headless>=4.8

# PyTorch + vision + timm (classifier backbones)
# Install a CUDA build matching your driver FIRST, e.g.:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
torch>=2.1
torchvision>=0.16
timm>=1.0
safetensors>=0.4

# Detector training (Ultralytics YOLO / RT-DETR)
ultralytics>=8.2

# OCR transfer baselines
# System binary `tesseract` + heb language pack is REQUIRED (see SYSTEM_REQUIREMENTS.md).
# Do not install the unrelated PyPI package named "tesseract".
pytesseract>=0.3.10
kraken>=3.1

# Optional: tensorboard logging
tensorboard>=2.14

# Optional: annotation interface
flask>=3.0

# Optional: Stage B regeneration (SD1.5 + ControlNet + IP-Adapter)
# pip install "diffusers>=0.27" "transformers>=4.40" "accelerate>=0.30" "controlnet_aux>=0.0.7"
