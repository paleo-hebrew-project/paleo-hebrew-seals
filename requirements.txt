# Core
PyYAML>=6.0
numpy>=1.24
Pillow>=10.0
tqdm>=4.65
pandas>=2.0

# PyTorch + timm (classifier backbones)
# Install a CUDA build of torch matching your driver, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
torch>=2.1
timm>=1.0
safetensors>=0.4

# Detector training (Ultralytics YOLO / RT-DETR)
ultralytics>=8.2

# OCR transfer baselines
tesseract>=0.1.3        # pytesseract; system Tesseract binary also required
kraken>=3.1              # Kraken OCR; Hebrew checkpoints loaded on demand

# Optional: tensorboard logging
tensorboard>=2.14

# Optional: annotation interface
flask>=3.0

# Optional: diffusion-based Stage B stylization (heavy; only needed to regenerate Stage B)
# diffusers>=0.27
# transformers>=4.40
# accelerate>=0.30
# controlnet_aux>=0.0.7
